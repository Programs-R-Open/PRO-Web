import pathlib
import os

ROOT = pathlib.Path(os.getcwd());
DIR = pathlib.Path(os.getcwd());
PROJ = pathlib.Path(os.getcwd());
FRONT = PROJ / "Frontend";
BACK = PROJ / "Backend";

P_PATH = pathlib.Path();
P_FILE = pathlib.Path();
P_NAME = "";